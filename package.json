{
  "name": "quoteme",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check:static": "node scripts/static-check.mjs",
    "test": "node --test --test-reporter=spec",
    "test:coverage": "node --test --experimental-test-coverage",
    "ci": "npm run check:static && npm test"
  }
}
